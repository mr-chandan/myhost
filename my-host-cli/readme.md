# pideploy-cli

> PiDeploy CLI — deploy full-stack apps to your Raspberry Pi

## Install

```bash
$ npm install --global pideploy-cli
```

## CLI

```
$ pideploy --help

  Usage
    $ pideploy <command>

  Commands
    login, register, deploy, apps, whoami, queue, logout

  Examples
    $ pideploy deploy https://github.com/user/repo
```
